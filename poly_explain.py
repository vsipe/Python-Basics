name = 'Jim' #str
print(len(name))

some_list = ['some', 'name'] # list

print(len(some_list))
# len is polymorphic, because it doesn't count all of the pieces of some list, 
# it only gives us the list of some list
